- **Service pods stop installing the client's npm dependency closure**: `[dependencies.npm]` packages exist only to build the client, which only the gateway does - yet every service pod installed them on every boot (500+ packages measured on a real app). Non-gateway bootstraps now strip the npm tables from their pod-local `jac.toml` before `jac install`; gateway and single-pod deploys are unchanged.
